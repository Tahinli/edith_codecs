# lane-mc64 r1 handoff

STREAM: `crates/ec-av1/fixtures/av1_192x128_8bit_intra64_in_inter.obu`
(md5 add55f625b6302c523455bb8662b19f5, sha256 e4cff46cb1700527c68efc866db27a2835705e949d646f73f0c9df577d594bd6).
The r2 aomenc recipe does NOT regenerate it (measured: 19003 bytes md5 278be003743daed5ea116cd41056537a). Use the fixture.

DEFECT, restated correctly:
decode-order frame 2, superblock at px(128,64) = mi(16,32), `EC_PART_VAL bsize=12 value=0`
(PARTITION_NONE 64x64), **INTRA block in an inter frame** (aomdec prints no EC_MODE/EC_IMODE
for it because `read_intra_block_mode_info` is uninstrumented; ours prints
`EC_MODE ... is_inter=0 skip=false`). Pre-deblock luma differs in exactly that 64x64
(3720/4096 samples, delta -12..+9), chroma bit-exact. Frames 0 and 1 exact.
The charter's `mode=16 ref0=4 mv0=(-340,-116)` belongs to FRAME 1's mi(16,32), which is
PARTITION_SPLIT there and decodes exactly. Do not chase MC: `EC_MC_TRACE=1` shows zero
MC/warp/compound calls for this block.

REPRO (both under systemd-run --user --scope -q -p MemoryMax=6G):
  EC_AV1_PREFILT_DUMP=<dir>/ref  ~/.cache/aom-oracle/build/aomdec --rawvideo -o /dev/null <obu>
  EC_NOMEMGUARD=1 EC_AV1_PREFILT_DUMP=<dir>/our  <target>/debug/examples/decode_probe <obu>
  cmp -l <dir>/ref.f2 <dir>/our.f2 | wc -l    # 3720
Scratch scripts: ~/.cache/mc64-tmp/{cmp.sh,gen.sh,gen2.sh}. Decode stops at frame 3 with
lane-intra64split's "a split intra strip whose transform unit is 64x32" refusal — unrelated.

NEXT: luma-only + chroma-exact on a 64x64 intra block points at the 64-length luma transform.
Bisect in this order: (1) TX_64X64 coefficient region (`av1_get_adjusted_tx_size` -> only the
top-left 32x32 is coded) and its eob domain; (2) the 64-point inverse (`transform.rs:276`
`hadamard` tail, `row_shift_wh(64,64) == 2`); (3) the 64x64 intra prediction itself.
`transform.rs:1412 a_64x64_transform_keeps_what_fits_in_its_coded_quarter` is the existing
64x64 self-check — it passes, so whatever is wrong is outside its span.
