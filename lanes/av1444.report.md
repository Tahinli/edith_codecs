# Lane av1444: AV1 4:4:4 (WIP)

Branch `lane-av1-444`, base `7f8d6cf6`. Worktree
`/home/tahinli/Documents/Code/Rust/edith_codecs-av1444`. Private
`CARGO_TARGET_DIR=$HOME/.cache/cargo-target-av1444`,
`TMPDIR=$HOME/.cache/av1444-tmp`. Not pushed.

Status: **WIP**. 4:4:4 is byte-exact on every SQUARE witness: 8-bit
profile 1 filters off AND on (loop filter + CDEF + restoration),
forced-64x64-block arms (exercises the four-TX_32X32 chroma loop),
10-bit profile 2 filters off AND on. 420 square arms stay IDENTICAL
(regression). 4:2:2 stays a named refusal. The inter sub8/64x64 chroma
paths at 4:4:4 are now ported (continuation 5, below): the entropy
ladder of `av1-profile1-444.ivf` is in sync END-TO-END and shown frames
0..17 are byte-exact vs aomdec/ffmpeg; frame 18 carries the first pixel
diff (2 U samples, +1, pre-filter — reconstruction, not entropy).
Full suite NOT run this round (budget wall).

## What changed (this continuation, on top of the staged WIP commit)

1. `decode_block` single-TU arm (was the prior agent's unstaged edit,
   debug prints removed): at 4:4:4 a 64x64 block's chroma is coded as
   four TX_32X32 units plane-major — confirmed bug-for-bug against
   libaom `av1_get_max_uv_txsize` (`av1_get_adjusted_tx_size`,
   blockd.h:1361) and the plane-major unit loop in
   `decode_token_recon_block` (decodeframe.c:970-1010).
2. **Root cause of the 36k chroma diffs** (the prior agent chased a
   phantom 64x64 block — the stream has none): `u`/`v`
   `set_tile_origin` hardcoded the 4:2:0 halving (`mi * 2`). At 4:4:4
   the halved `tile_x1` clamped `PlaneBuf::edges`' `across` below the
   block's own x for every chroma block in the right half of the tile
   (and `tile_y1` the left edge in the bottom half), so those blocks
   predicted with edges missing (left-only/above-only DC). Localized by
   proving the entropy ladder identical (1596/1596 rng rungs vs the
   instrumented aomdec), then diffing `OUR_PRED`/`EC_PREDOUT8` for the
   first divergent CFL block: aom DC 129, ours 145, traced to
   `above=None` in `edges()`. Tile bounds now `mi * 4 >> ss`; identical
   at 4:2:0.
3. Loop filter: `deblock_plane_span` clipped chroma to
   `frame_width/2 × frame_height/2` (another hardcoded halving) — at
   4:4:4 the filter never touched three quarters of each chroma plane.
   Crop now follows `ss`. (CDEF and restoration were already exact:
   isolating arms `--loopfilter-control=0` vs `--enable-cdef=0` /
   `--enable-restoration=0`.)
4. Rect/1:4 4:4:4 port (in progress):
   - `decode_block_rect4` (32x8/8x32 strips): chroma table was
     hardcoded 4:2:0 (`Chroma8`, SCAN_16X4/4X16, mode-derived tx_type
     with no size clamp → ADST-at-32 panic). Now shape-keyed
     (4:4:4: `Chroma16`, SCAN_32X8/8X32) with the
     `av1_get_ext_tx_set_type` DCT_ONLY clamp at square-up ≥ 32
     (blockd.h:1278).
   - Inter rect chroma: a 64-axis strip at 4:4:4 keeps its 64-px chroma
     axis, capped to 32-wide units (`av1_get_max_uv_txsize`), so it
     codes TWO units plane-major. New `read_inter_rect_chroma` helper
     (both `decode_inter_block` rect readers call it);
     `read_inter_plane_rect`'s chroma skip_ctx takes the +3 (libaom
     +10) offset for multi-unit blocks; `rect_inter_chroma_set` gained
     the (32,32) → `Chroma32` entry.
   - `decode_rect4_16_strip` (16x4/4x16 strips): at 4:4:4 there is no
     4:2:0 chroma pair — every strip is its own chroma reference
     (`is_chroma_reference`: no mi-parity test at ss 0/0, strip's own
     chroma ≥ 4x4). `has_chroma` is now true for every strip at ss 0/0
     (key-frame caller `decode_rect4_16` AND the inter
     `inter_strip_chroma` construction), the pair origin/tables/CfL
     window anchor at the strip itself, and the inter `prev`
     (both-strips MC) is disabled at 4:4:4.
   - Leftover instrumentation from the staged WIP removed (EC_64*,
     EC_TILE); `EC_RECTCHROMA_GAP` stays (deliberate diagnostic per its
     comment).

## Byte-exactness (all vs aomdec, ffmpeg cross-checked; recipes in
`$HOME/.cache/av1444/witness_*.sh`)

| stream | result |
| --- | --- |
| profile 1, filters off, square 128x128 2f | IDENTICAL (98304 B) |
| profile 1, filters ON (LF+CDEF+LR), square | IDENTICAL |
| profile 1, filters off, forced 64x64 blocks (`gradients --min-partition-size=64`, 8 64x64 leaves) | IDENTICAL |
| profile 1, filters on, forced 64x64 blocks | IDENTICAL |
| profile 2 10-bit, filters off, square | IDENTICAL (196608 B yuv444p10le) |
| profile 2 10-bit, filters on, square | IDENTICAL |
| profile 0 420, filters off, square | IDENTICAL (regression) |
| profile 0 420, filters on, square | IDENTICAL (regression) |
| `av1-profile1-444.ivf` (320x240, rect/1:4 shapes) | decodes 24+ frames, then **panics** `reconstruct_rect len 64 index 64` (decode.rs:3619, `exec_intra` track-caller) after `EC_RECTCHROMA_GAP luma=64x16 tx=16x16` — the 64-axis intra strip's split-transform chroma path is the next defect |

## Still refused or unproven

| shape | disposition |
| --- | --- |
| 4:2:2 (`ss_x != ss_y`) | refused by name. Gate re-scoped, not re-run |
| inter rect2 sub8 (8x4/4x8 pieces) at 4:4:4 per-piece chroma | unported — `decode_inter_sub8_rect2` still codes the 4:2:0 group unit |
| `decode_inter_sub8_split4` per-piece chroma eob CDF adaptation | wall (below), first mismatch deep in frame 1 |
| full `cargo test -p ec-av1` | NOT run this round. Do not claim green |
| 0-warning parity | build is warning-clean (`cargo build --release -p ec-av1 --example decode_probe`); full-suite parity not measured |

## Continuation 4 (2026-09-23, Av1X444Cont4)

1. **Edge panic fixed (Rauf's clamp, verified bug-for-bug, committed)**:
   `around_mi`/`around_mi_rect`/`luma_skip_ctx_rect` clamp each walk to
   `mi_cols`/`mi_rows`. libaom facts checked: the above/left entropy+coeff
   arrays are `aom_calloc(aligned_mi_cols, ..)` per tile row
   (alloccommon.c:437-448, `ALIGN_POWER_OF_TWO(mi_cols, MAX_MIB_SIZE_LOG2)` =
   exactly 80 for the 320 px fixture) — zero-initialized and never written
   beyond the frame extent, and `get_txb_ctx_general` (txb_common.h:281)
   walks only the TXB's own span, so beyond-frame cells contribute ZERO to
   every context on libaom's side too. The clamp reproduces exactly that;
   there is no edge sample extension for these entropy-context arrays. The
   fixture's `reconstruct_rect len 64 index 64` panic is gone: the probe
   decodes all 60 frames, exit 0.
2. **Fixture frame 0 (first shown frame) still byte-exact** vs ffmpeg
   yuv444p after the above (re-verified every round).
3. **decode_idx 1 (hidden inter, base_q 57, show_frame=0) root-caused and
   partially fixed**: the frame's first partition reads
   (`w64/w32/w16` all SPLIT at mi (0,0)) were in sync, but aom reads an
   **8x8-level `partition_w8` symbol** before each 8x8 leaf of the inter
   walker too (the keyframe walker already read it; the inter walker reads
   it silently — the missing *per-piece chroma* was the real gap):
   at ss 0/0 `is_chroma_reference` is unconditional
   (av1_common_int.h:1459 — `!subsampling_x/y` force both disjuncts true),
   so every 4x4 piece of a `partition_w8 == SPLIT` group codes its OWN 4x4
   U and V unit right after its luma, plane order 0,1,2 per leaf.
   `decode_inter_sub8_split4` coded the 4:2:0 group unit instead. Fixed:
   per-piece chroma MC (piece's own mv/filters, ss 0) + per-piece
   `read_inter_plane` + `record_mi_chroma`; the group unit is now gated
   `!chroma_444`, with `record_uv_mode_mi`/`record_strip_palette` hoisted so
   they run at both subsamplings. Result: the coefficient-step ladder
   against instrumented aomdec moved from first mismatch at step 387 to
   step 925 (15 chroma TUs + the whole (0,2) group and (2,0)/(2,2) 8x8
   blocks now bit-identical; all `partition_w8` values match through
   mi (2,6)).
4. **Current wall (precisely localized)**: decode_idx 1, the (2,6) split4
   group's piece-0 U TU (4x4, ss 0/0). Both sides read the identical
   `all_zero` symbol (rng 34242, value 0), then the `eob_pt` read diverges:
   aom eob=4, ours eob=2, from identical pre-state. Instrumented oracle
   (`EC_EOBPT_CDF` print added to decodetxb.c, `ninja aomdec` rebuild) shows
   aom's `eob_flag_cdf16[PLANE_UV][0]` contents at that read =
   `[30271, 28672, 23902, 15775]` — heavily adapted — while ours
   (`eob_pt_16_chroma`, bound `cdf_state.rs:1841`) is still near its default
   `[6764, 8165, 12733, 17250]`. So aom's chroma-4x4-2D eob CDF instance
   receives adaptation from reads our model routes elsewhere (or misses):
   the next suspects are (a) `decode_inter_sub8_rect2`'s per-piece chroma
   (still the 4:2:0 group unit — aom codes per-piece 8x4/4x8 chroma units,
   same 16-coeff eob class), and (b) the intra 16x4/4x16 strips' chroma
   units in inter frames (`intra16x4_in_inter` 16x4=68 4x16=24), whose
   4x4-class eob reads must land on the same `eob_flag_cdf16[UV][2D]`
   instance. Tooling left in place: `EC_DBG_SUB8=1` (per-b8 partition
   value/pre/post rng, per-piece skip/is_inter), `EC_TRACE_PART=1`
   (EC_PART-style partition walk), oracle-side `EC_EOBPT_CDF=1`
   (per-4x4-eob CDF contents + mi). Comparison harness:
   `EC_TRACE_COEFF=1 aomdec` vs `EC_TRACE_COEFF=1 decode_probe` on
   `$HOME/.cache/av1444/f2.obu` (2-frame cut), diff with the 15
   instrument-only chroma `tx_type` lines dropped.
5. Regression arms after the change: 420 square IDENTICAL, 444 square
   filters off IDENTICAL, 444 square filters on IDENTICAL, forced-64x64
   444 IDENTICAL (witness scripts, rerun this round).
6. **Frame-output policy note**: `decode_probe` writes only SHOWN frames
   (`is_shown` gate); aomdec/ffmpeg likewise. The first shown frame is
   decode_idx 0; decode_idx 1..5 are hidden alt-ref/LF-update frames — they
   still must decode bit-exactly because every later shown frame references
   them.

## Next

1. Frame 18's 2-pixel U diff (see continuation 5's wall): confirm
   whether the covering block is a UV-PALETTE block (`intra_in_inter_palette:
   uv=76` counter fires) — compare `read_palette_colors_uv`
   (decode.rs:8979, libaom decodemv.c:509) delta decoding vs libaom, or
   if not palette, dump the residual/inverse of the covering 4x4.
2. Then frames 18..59: iterate the same way (ladder is already
   end-to-end clean on the 24-frame cut, so expect reconstruction-only
   walls; re-diff per frame group).
3. Flip the refusal gate (`a_non_420_subsampled_sequence_header_is_
   refused_by_name` re-scope) to a fixture witness gate; witness or
   refuse inter square 64x64 444 (the forced-64x64 witness already
   passes — that arm is intra-only, the inter 64x64 path is new).
4. Full suite `--test-threads=1` (NOT run this round), 0-warning
   parity (build IS warning-clean), update this report.

## Continuation 8 (2026-09-23, Av1X444Cont8 — lane closed: fixture byte-exact end-to-end)

Round-7's ONE-symbol wall is RESOLVED; `av1-profile1-444.ivf` decodes ALL 60
shown frames byte-exact vs ffmpeg AND aomdec (three-way sha256
`c3f95bca00f0ba5ea9f7071f5645edd910b590efebea2d73398017d10dc272d7`).

1. **Root cause (class `override-slot-on-one-arm` via a zero grid):** the
   64x64-inter four-TX_32X32 chroma arm read each unit correctly and stamped
   each unit's context IMMEDIATELY (`record_mi_chroma(cu_mi, 32, 32, ..)`),
   then set `mu_chroma = true` and handed `Grid::Zero` to `u_grid`/`v_grid`.
   The block tail then painted 0 over those correct stamps TWICE:
   `record_rect_mi`'s whole-block chroma write (state of the zero grid) and
   the `mu_chroma_units` re-stamp, which sliced its "units" out of the SAME
   zero grids. Every block after a 64x64 inter block thus read chroma
   coefficient contexts 0 across the block's whole span. Oracle trace at the
   wall (`EC_DBGCTX`, rebuilt `build-c/aomdec`): `mi=52,16 plane=1 tx=2
   above0=0 left0=21 base=1 off=7 ctx=8 cellsA=0,0,0,0 cellsL=21,21,21,21`
   — aom's left cells for the mi(52,16) U TX_16X16 hold the mi(48,0) block's
   U TX_32X32 (48,8) unit's running cul (raw cell 21; only zero-vs-nonzero
   matters for `get_entropy_context`), ours held 0 → ctx 0 vs 8.
2. **Fix (both copies of the arm — single-ref and compound):**
   - The arm now assembles the four per-unit coefficient grids per plane
     into `u_grid`/`v_grid` (`mu_units` + `Grid::Own`, same pattern as the
     128-root path), so the block-tail re-stamp slices REAL states.
   - `mu_chroma_units` collection enumerates FOUR TX_32X32 units per 64x64
     mu chunk at ss 0/0 (one at 4:2:0 — byte-identical to the old loop),
     each at its own mi origin, own `(32 << ss)`-px entropy-cell span, own
     32x32 quadrant of the assembled grid; the stamp loop uses each unit's
     own span (was a blanket 64 px). Lossless branch unchanged.
3. **Witness gate (all fresh runs, ffmpeg `-y` added to the scripts — the
   old scripts could silently compare a STALE ffmpeg reference when the
   output file existed):**

   | stream | result |
   | --- | --- |
   | `av1-profile1-444.ivf`, all 60 shown frames | IDENTICAL (ffmpeg == aomdec == ours, sha256 above) |
   | 444 square 8-bit, filters off | IDENTICAL |
   | 444 square 8-bit, filters ON (LF+CDEF+LR) | IDENTICAL |
   | 444 square 10-bit, filters off | IDENTICAL (new `witness_444_10b.sh`) |
   | 444 square 10-bit, filters on | IDENTICAL |
   | 444 forced-64x64 blocks (`gradients --min-partition-size=64`) | IDENTICAL |
   | 420 square, filters off (regression) | IDENTICAL |
   | 4:2:2 | refused by name (suite gate, unchanged) |

   **Inter square 64x64 at 4:4:4 is witnessed by the fixture itself**: the
   64x64 PARTITION_NONE inter block at mi(48,0) of decode idx 52 codes the
   four-unit U/V TX_32X32 loop, and every later block in rows 48..59
   (including f52's own mi(52,16) read) consumes its per-unit contexts —
   byte-exactness of frames 52..59 is that consumption proof. The
   forced-64x64 generated arm stays intra-only and passes.
4. Regression arms quoted above; `hg_*` 10-bit, mono 60f, qmatrix-refused
   and cdf-exact gates ride the full suite (final line appended below).
5. Named gap, unchanged: an inter 128x128 block at 4:4:4 still codes ONE
   chroma unit per mu chunk where libaom codes four (`side > 64` arm) —
   unported symbol stream, same disposition class as the rect2 sub8 row.
   The collection fix makes the CONTEXT machinery unit-complete for it, but
   the stream port is not attempted.
6. Full suite `--test-threads=1` (hub-supervised): see the suite line at
   the bottom of this file.

## Continuation 7 (2026-09-23, Vefa — budget-wall WIP)

Round-6's wall is RESOLVED; the fixture moved from "shown 18 seed" to a new,
exactly-localized wall at decode idx 52 (shown 49).

1. **Root cause (leaf B was a victim, class stale-neighbour-mode):** leaf
   (42,58)'s chroma DC base 145-vs-146 traced to its LEFT edge, not to the DC
   gather. Our `dc()` already carries libaom's `(sum + count/2) / count` bias;
   the sums differed because leaf A (42,56) output was wrong: its left
   neighbour (42,55) coded `uv=SMOOTH_H` (11) on a 4x4 intra-in-inter piece,
   but the inter sub8 group tails (`decode_inter_sub8_split4` /
   `decode_inter_sub8_rect2`) stamped `DC_PRED` over the whole 2x2-mi group's
   uv-mode neighbour cells AFTER the pieces had recorded their own modes
   (trace: pieces 12/11/9/12 at (42,54..55) wiped to 0). libaom keeps every
   sub-block's own `mbmi->uv_mode`. Consequence chain: aom
   `get_intra_edge_filter_type`=1 for leaf A vs our 0 → libaom's type-1
   upsample cap (`blk_wh <= 8`) leaves the edge alone while our type-0 cap
   (`blk_wh <= 16`) UPSAMPLES it for V_PRED p_angle=87 (dx=3): base row0
   [174,172,**179**,189,185,**183**,195,**182**] vs aom
   [174,172,**178**,189,185,**182**,195,**183**] — every later sample
   reproduced numerically. Leaf B's left column then differed at rows
   168/170, and the (correct) biased DC rounded 145 vs 146.
2. **Fix:** gate both group-tail uv stamps on `!chroma_444`. At ss 0/0 every
   piece already writes its own state (inter pieces through
   `record_inter_rect_mi`'s is_inter DC stamp, intra pieces through
   `decode_intra_sub8_leaf`'s chroma_444 arm); the group stamp is 4:2:0-only.
3. **Verified:** w24 cut — ALL 26 decode-order frames prefilt byte-exact
   (was f19=122 U/V diffs, f20, f22). Full fixture — decode 0..51 (shown
   0..48) byte-exact vs ffmpeg yuv444p; round-6's f1 single V sample is gone.
4. **New wall (localized, next round):** decode idx 52 first diverges at
   Y(128,192) (ours 210, aom 209); prefilt 21735 diffs; the symbol ladder
   desyncs INSIDE frame 52. Facts: aom f52 walk at mi(52,16) is
   BLOCK_16X16 PARTITION_NONE (`EC_PART bsize=6 value=0`); luma ONE
   TX_16X16 (matches ours); then chroma U TX_16X16 reads all_zero with
   `txb_skip_ctx=8` = base 1 + offset 7 (`get_txb_ctx_general`: chroma
   offset 10 iff plane_bsize bigger than the transform, else 7;
   plane_bsize==BLOCK_16X16==tx → 7). OUR same-position read: all_zero
   **ctx=0** — `decode_block`'s cn==1 chroma arm passes offset `None`
   (only cn>1 passes `Some(3)`), and our above/left entropy flags read
   0 where aom's combine gives 1. Pinned with the rebuilt oracle's new
   `EC_DBGCTX` print (cellsA/cellsL per unit): aom's U TX_16X16 cellsA
   = 0,0,0,0 but cellsL = **21,21,21,21** — the left cells at mi rows
   52..55 were written by the 64x64 mi(48,0) block's U TX_32X32 (48,8)
   unit with cul 21, while OUR left cells for the same rows/plane read 0.
   So the desync is ONE symbol (the U all_zero) and the suspect is our
   per-unit chroma context WRITE for the 64x64 inter path
   (`record_mi_chroma` span/cul derivation) or its cell mapping — our
   (48,8) U grid has its coefficients in the unit's TOP half (grid rows
   8..15), so a correct bottom-cell write would be 0 and the 21 must
   come from aom's cul derivation over the whole 32x32 unit (cul is a
   running count, not per-cell) — verify `read_coeffs`' cul accounting
   against libaom's `read_coeffs_txb` loop.
5. **Infra note:** the oracle's `EC_AV1_POSTCDEF_DUMP` index is off by one
   (aom pc.fK = decode K+1); under that alignment CDEF is byte-exact frames
   44..51, and post-deblock is byte-exact through idx 51 — the wall is NOT
   loop-filter related.
6. NOT done this round (budget): regression arms (420/444 square, forced-64,
   mono, 10-bit, qmatrix, cdf) and the full suite. Committed at the wall;
   tree clean; no push.



Round-5's frame-18 target was mis-aimed: the visible 2-pixel U diff is
INHERITED. Per-frame prefilt diff (EC_AV1_PREFILT_DUMP, crop-aware):
f0..f18 byte-exact (f1: one V sample (224,272) ours +1, non-propagating),
**f19 (hidden) = 122 U/V diffs** around rows 168..179 / cols 225..247,
f20 = exactly the known 2 U samples, f22 = 50 U diffs. The root lives in
hidden frame idx 19; f20/f22 inherit it through MC.

Enum correction (class stale-assumption): libaom's UV_PREDICTION_MODE has
UV_CFL_PRED **at index 13** (after PAETH/SMOOTH_H), not 1; uv=1 is
UV_V_PRED. So f19's leaf (42,56) is V_PRED (not CFL) and leaf (42,58) is
**CFL** (not smooth/palette). Round-5's read_palette_colors_uv suspicion
was a dead end — the (42,56) block reads no palette and (42,58) reads no
palette; both sides' PALSYN rows agree rng-for-rng (47240 anchor).

Verified bit-exact this round (instrumented C-only oracle build at
~/.cache/aom-oracle/build-c plus new EC_DQCOEFF / EC_PREDB8 / EC_TXIN8 /
EC_TXOUT8 / EC_FINAL8 dumps): coefficient levels+signs, dqcoeff grids
(OUR_DQ == aom EC_DQCOEFF after reading aom's buffer column-major),
qindex 123 (delta-q applied correctly on both sides), quant tables, the
dequant formula, the 1D idct8 + row/col shift composition (python replica
matches oracle TXIN/TXOUT exactly), CFL weights/scaling, and the leaf's
residual: OUR res0 == aom TXOUT row0 exactly
([14,-31,-67,-63,-17,5,-17,-23]).

**Localized defect:** f19 leaf (42,58) V (UV_CFL_PRED, alpha=-1): aom
composes base 146 + CFL + residual; ours composes base **145** + identical
CFL + identical residual → every sample exactly -1. The -1 is in the
chroma DC_PRED base: aom 146 = round((Σabove 960 + Σleft 1369)/16) with
Σleft including (175,231)=166; our 145 is one rounding/edge-sample step
off (candidates: missing rounding bias (2329>>4), or the left edge sample
set). Our (168,231)=180 vs aom 181 is leaf A's own diff, not leaf B's
edge (smooth_v not in play; leaf B's below-left (175,231)=166 identical).

Still open (next steps): 1) pin the DC_PRED edge/rounding divergence in
our intra DC path for this leaf (DBG192 build committed: EC_DBG192=1
dumps pred0/res0 at x=224/232 y=168); 2) leaf (42,56) V_PRED's scattered
±1 U/V diffs (base = clean above-row copy, residual identical ⇒ its own
edge/availability wrinkle — needs the same treatment); 3) f1's single V
sample; 4) re-run the fixture end-to-end + witness gate + regression
arms + full suite after the fix.

Oracle/oracle-build notes: EC_AV1_PREFILT_DUMP crops at true dims on the
aom side; ours writes padded planes (Y 240x320, U/V stride 320 with a
16-row top border, offsets 81920 / 163840 in the fN file). aomdec's
EC_COEFF `pos` is a zigzag raster pos; EC_COEFF_VAL is per-unit, not
per-coefficient. The f9 unit at the same mi/rng anchor was a red herring
— always pair units by EC_FINAL8 pixel values, not by rng.

## Continuation 5 (2026-09-23, Semiha)

Round-4's wall was misdiagnosed: aom's divergent eob read was
`eob_flag_cdf16[PLANE_UV][ctx=1]` (the trace prints `ctx=1`; the
"[UV][2D]" reading was wrong), adapted in frame 1, while ours bound the
ctx-0 row. Chasing it surfaced four real defects, all entropy-side; the
whole-stream ladder then went clean and the wall moved to a
reconstruction rounding.

1. **`decode_inter_sub8_split4` per-piece chroma now inherits the
   piece's own luma `tx_type`** (was hardcoded `Some(DctDct)`): an inter
   block's chroma never codes its own symbol but inherits the colocated
   luma transform's coded type (`av1_get_tx_type`, blockd.h:1291), which
   decides `eob_multi_ctx` (decodetxb.c:214: `(tx_class == TX_CLASS_2D)
   ? 0 : 1`). A piece whose luma picked V_DCT/H_DCT desynced the chroma
   eob read one row. This was the round-4 wall.
2. **`decode_inter_sub8_rect2` per-piece 8x4/4x8 chroma ported** (round
   4's suspect (a), confirmed by trace: aom codes a chroma TX_8X4 at
   mi (3,8) that ours skipped): at ss 0/0 every piece codes its own U/V
   units right after its luma, predicted from the piece's own mv/filters
   at the piece's own pixel position, inheriting the piece's first
   luma TU's tx_type (`TxbSet::ChromaRect8x4`). The 4:2:0 group unit is
   gated `!chroma_444`; `record_uv_mode_mi`/`record_strip_palette` are
   hoisted to run at both subsamplings (same pattern as split4's r4
   port). Skip pieces push zero-residual MC and zero chroma context.
3. **`decode_intra_sub8_leaf` chroma is per-piece at ss 0/0** (round 4
   had `chroma_444 || i == 3` on the split caller only, and the leaf
   still anchored "the group's one 4x4 unit" at the group origin): the
   leaf now anchors at the piece's own position with the piece's own
   shape — a 4x4 unit under SPLIT (`Chroma4`, `read_plane`), the leaf's
   own TX_8X4/4X8 unit under HORZ/VERT (`ChromaRect8x4` via
   `read_coeffs_rect`, tx_type from the uv mode via
   `Intra_Mode_To_Tx_Type`, CFL alpha window over the piece's own
   footprint). The rect2 caller passes `chroma_444 || i == 1`. uv_mode
   neighbour publishing is per-piece at ss 0/0
   (`record_uv_mode_mi(lmi, w_mi, h_mi, ..)`).
4. **A 64x64 INTER block codes FOUR TX_32X32 chroma units, plane-major,
   at ss 0/0** (was one 64x64 read through 32-class tables): each unit
   carries the +10 `get_txb_ctx_general` offset (chroma plane block
   larger than the transform; our `txb_skip_chroma_32` holds those rows
   at +3, so `luma_skip_ctx: Some(3)`, the lane-sb128b r3 convention as
   the 128 mu path), predicts from its own 32x32 window of the block's
   MC buffer, and records chroma context per unit immediately
   (`record_mi_chroma`), with `mu_chroma = true` so the block-level
   record does not overwrite the per-unit states.

Ladder evidence (`ladder.py` left in `$HOME/.cache/av1444/`; two-pointer
walk on `(tag, rng)` tolerating the known instrument asymmetries — aom's
chroma `tag=tx_type` prints, `post_golomb` echo rows, ours' `sign_rect`
batching and golomb-tail br rows): on the 24-frame cut
(`w24.obu`), ours 358473/358473 steps, aom 355132/355132, zero
unexplained mismatch — the ENTROPY stream is end-to-end in sync.
Round-4's named suspects (b) — intra 16x4/4x16 strips feeding
`eob_flag_cdf16[UV][2D]` — did NOT materialize: TX_16X4 chroma is
64-coeff (cdf64 class); the cdf16 UV instance is fed only by split4
piece chroma, all of which now bind correctly.

Pixel state (`w24.our.yuv` vs `w24.ref.yuv`, 24-frame cut): shown
frames 0..17 byte-exact; frame 18 differs in exactly TWO U samples,
(168,230)=218 vs 217 and (168,231)=204 vs 203 (+1 both), confirmed
PRE-loop-filter via `EC_AV1_PREFILT_DUMP` (decode idx 20 == shown 18) —
a reconstruction defect, not entropy. Full 60-frame run: frames
0..17 exact, first diffs frame 18 (U,2 px), then sparse U diffs frames
19..37, Y diffs from frame 49 (drift accumulating behind the frame-18
seed). Prediction of the covering block is IDENTICAL to the oracle
(aom `EC_PREDOUT8 mi_row=42 mi_col=57 plane=1 sum=3280 row0=205,206,209,200`
== ours), so the residual/dequant/inverse or a UV-palette colour write
is the remaining suspect: the block is adjacent to `intra_in_inter_
palette uv=76` territory and the (224,168) 8x8 leaves no `OUR_TXTYPE`
trace, so it is likely a UV-PALETTE block — compare `read_palette_
colors_uv` (decode.rs:8979, libaom decodemv.c:509) delta/rounding.

Regression arms this round: 444 square filters off IDENTICAL, 444
filters on IDENTICAL (aomdec==ffmpeg cross-check), 420+forced-64x64
IDENTICAL. 10-bit arms and mono NOT rerun this round. Full suite NOT
run (budget wall). Build is 0-warning
(`cargo build --release -p ec-av1 --example decode_probe`).
