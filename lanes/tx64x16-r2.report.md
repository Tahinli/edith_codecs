# lane-tx64x16 r2 — the SB-level 1:4 pair is GREEN; r1's "17 failures" were a stale base

Branch `lane-tx64x16`, rebased dc5553f -> `5811ec5` -> **`6d86d3a`** (current main
at the time of writing). Commits: `3435835` (r1 wiring, replayed), `23aa4b3`
(r1 report), `d1f71b6` (this round).

## Headline

**r1's red gate was a STALE BASE, not a defect.** Nothing in the 1:4 wiring
changed this round. Rebasing onto main turns
`a_real_aomenc_stream_with_a_superblock_level_1to4_partition_decodes_pixel_exact`
GREEN (both arms hard-asserted, 40 aomenc attempts), and the r1 suite's
"17 failed" collapses to 2 — neither of them reachable by this lane's diff.

Class for the ledger: **stale-premise lane** (5th instance) — a lane whose gate
is red against a base that is 20 merges behind measures the base, not its own
change. Re-measure the premise on today's main BEFORE bisecting your own diff.

## The 17, enumerated (charter's first job)

They do not exist on the rebased branch. Full `cargo test -p ec-av1 --lib`
(`EC_AV1_REQUIRE_AOMENC=1`) on the rebase-onto-5811ec5 tree, before any fix of
mine: **290 passed, 2 failed, 27 ignored**, the two being

1. `decode::tests::nz_map_ctx_offset_tables_match_the_rect_rule` — **RED ON
   MAIN**, not caused by this branch (`git diff --stat main..HEAD` touches
   `decode.rs` (partition arms + counters + scans), `refusal_inventory.rs`,
   `stream.rs` only; the table, its consumer and this test are all main's).
   Root cause: the test read `NZ_MAP_CTX_OFFSET_32X64`/`_64X32` as
   `table[col][row]` — lane-rectsplit r2's column-major premise, which r3
   disproved and reverted in the *tables* without reverting the *test*. The
   tables as they stand are display-major and satisfy libaom's generating rule
   (`txb_common.h:199-209`) read `[row][col]`, which is exactly how the only
   real consumer reads them (`base_ctx`, `TxClass::TwoD` arm,
   `crates/ec-av1/src/decode.rs:1829`) and the reading every merged rect gate
   is green under. FIX: read the test in the consumer's order. Table content
   untouched — zero risk to the 64x32/32x64 path.
   Verification of the direction: for `TX_32X64` (w<h) libaom's rule gives
   `11+ctx` for every `row < 2`; our const's row 0 is `[0,11,11,11,11]` and row
   2 is `[6,6,21,21,21]` = `row+col<4 -> 6`, i.e. display-major and correct.
2. `gate_coverage::tests::never_exercised_8bit_matches_the_gate_recipes` —
   caused by this branch, and by design: this lane's gate is the first to pass
   `--enable-1to4-partitions=1` at 8 bit, so the 8-bit coverage hole is retired.
   FIX: entry deleted from `NEVER_EXERCISED_8BIT`; the 10-bit entry stays and
   now says "10-bit hole only".

Bisecting the corner generalisation vs the scan additions (the charter's plan)
was therefore not needed and not run — the premise it rested on ("a change that
breaks 17 gates") was false.

## Rebase conflict resolutions (main had moved onto the same lines)

- `decode.rs` SB partition dispatch: BOTH arms kept — main's
  `PARTITION_HORZ_A/_B/VERT_A/_B` (part32 r4) and this lane's
  `PARTITION_HORZ_4/VERT_4`; `reduced_tx_set` (new on main) threaded into the
  1:4 `decode_block_rect64` calls.
- `decode.rs` counters: both `SB_AB_HITS` and `SB_RECT4_{HORZ,VERT}_HITS` kept.
- The `_` arm of the SB partition match is now unreachable (all ten partition
  values are coded at 64x64); it refuses "a superblock-level partition value
  outside PARTITION_NONE..PARTITION_VERT_4" and `refusal_inventory` lists that
  string instead of the retired 1:4 one.
- `stream.rs`: git had spliced this lane's gate INTO main's AB gate. Resolved by
  taking main's file whole and re-appending the 1:4 gate function verbatim.

## Refusal lifted

`"a superblock-level 1:4 partition (PARTITION_HORZ_4/VERT_4 at 64x64, four
64x16/16x64 strips)"` — gone from `decode.rs` and `refusal_inventory.rs`, with
the gate above green. `refusal_inventory` + all five `gate_coverage` tests green.

## EVIDENCE

EVIDENCE: ~/.cache/tx64-sb-gates.log | `cargo test -p ec-av1 --lib -- superblock_level` with EC_AV1_REQUIRE_AOMENC=1 on d1f71b6 | 3 passed, 0 failed, 1 ignored (rect64q drift gate, ignored on main): the 1:4 gate AND both sibling SB gates (horz_vert, ab) green — SIBLING GATES rule satisfied
EVIDENCE: ~/.cache/tx64-suite-r2.log | full `cargo test -p ec-av1 --lib`, rebase onto 5811ec5, before my fixes | 290 passed / 2 failed / 27 ignored — the two named above, replacing r1's unenumerated 17
EVIDENCE: /tmp/claude-1000/-home-tahinli-Documents-Code-Rust-edith-codecs/b6d8a07f-96a4-4bbb-b378-af9ae25cf7c9/scratchpad/troy.obu (sha256 32e0619ca9fd9ae0e16072c66013757aa7abfedccc99bc6d3509f6e213195226, `ffmpeg -ss 300 -t 0.4 -c:v copy -f obu`) | `cargo run -p ec-av1 --example decode_probe` on d1f71b6 | Troy no longer stops at the 1:4 refusal; new stop is "a superblock-level HORZ/VERT strip with a split transform (per-unit rect prediction is not ported)" — the refusal lane-rectsplit r4 (7b55227) lifts
EVIDENCE: ~/.cache/tx64-suite-r2b.log | full `cargo test -p ec-av1 --lib` on d1f71b6 (rebased onto 6d86d3a, both fixes in) | see "Suite totals" below

## Open, with dispositions

- **fix-now, NEXT ROUND (Hunger Games critical path)**: the **32x32-level**
  `PARTITION_HORZ_4/VERT_4` (`decode.rs:9384`, refusal "a 32x32 partition type
  this decoder does not code (value={part32})", value 8 = HORZ_4). Shape of the
  work, already scouted: four `decode_block_rect(at32, 32, 8)` /
  `(8, 32)` strips at `quarter_step` = 1 mi-pair, same `i > 0` frame-edge break
  as the 64-level arm. Luma TX_32X8/TX_8X32 are their own adjusted size
  (`av1_get_adjusted_tx_size` leaves them alone), entropy ctx = TX_16X16 and
  `txsize_log2_minus4` = 4 (256 positions) => `TxbSet::Luma16` exactly, and the
  scans **already exist** in this branch: `SCAN_32X8` / `SCAN_8X32`
  (`decode.rs:1952`/`1969`). What is genuinely missing is CHROMA 16x4 / 4x16:
  `TxbSet::Chroma8` (ctx TX_8X8, 64 positions) plus two new scans
  `default_scan_16x4` / `default_scan_4x16`, transcribed with this repo's
  column-major -> row-major rule (`row*w+col = (p % h)*w + (p / h)`; validate
  the rule by regenerating an existing scan first — a naive copy installs the
  transpose).
- **deferred(the 32-level arms above)**: a 10-bit twin of the 1:4 gate. The
  `NEVER_EXERCISED_10BIT` entry for `enable-1to4-partitions` is still live and
  now says so explicitly.
- **flagged, resolved**: r1's "pre-existing seed-55 defect (3944 wrong luma,
  zero 1:4 blocks)" did not reproduce on the rebased tree — the 1:4 gate runs
  all 40 seeds of that same recipe (seeds 42..81, `--min-partition-size=16`) and
  every decoded attempt now compares pixel-exact, including seed 55. It was
  another symptom of the stale base (main has since landed inter8/rectsplit/
  txselect/seg fixes). No separate pin is owed.
- **accepted**: `main` moved again during the round (rectsplit r4 `7b55227` is
  not yet merged). If it lands first, its `base_ctx` rewrite may delete
  `NZ_MAP_CTX_OFFSET_32X64/_64X32` outright and conflict with my one-line test
  fix — resolve by taking r4's version, the defect is the same one.

## Suite totals

`cargo test -p ec-av1 --lib` with `EC_AV1_REQUIRE_AOMENC=1`, on `d1f71b6`:
**295 passed, 0 failed, 27 ignored, 0 filtered out** (1234s under concurrent lane load).
Both failures above are fixed; no new failure introduced. r1's 256/17/22 is superseded.
