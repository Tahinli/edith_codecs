# lane-sb128c r2 -> r3 handoff

Tip `lane-sb128c` (worktree `edith_codecs-sb128c`, `CARGO_TARGET_DIR=$HOME/.cache/cargo-target-sb128c`).
Full detail: `lanes/sb128c-r2.report.md`.

CLOSED in r2: the 128 inter NONE desync = `decode_inter_block`'s intra tail read the 14-symbol
`uv_mode_cfl` CDF for a 64x64 intra block in an inter frame; libaom uses `is_cfl_allowed(xd)`
(`<=32x32`, blockd.h). Fixed at `decode.rs:18713`.

ONE open defect, fully characterised, blocking the r1 gate:
`an INTRA-coded 128x128 block inside an inter frame` (`decode.rs:16396`).
- Repro: `decode_probe $HOME/.cache/sb128c-work/g8.obu` (recipe in `lanes/sb128c-r1.report.md`).
- It is REAL, not our desync: aomdec `EC_TRACE=1 EC_TRACE_COEFF=1` shows
  `EC_PART_VAL mi_row=32 mi_col=32 bsize=15 value=0` with no `EC_MODE`, then per 64x64 mu chunk
  `EC_COEFF plane=0 tx_size=4` (TX_64X64), `plane=1 tx_size=3 ctx=10`, `plane=2 tx_size=3 ctx=10`
  (`$HOME/.cache/sb128c-work/aomdec.all:18304`).
- Work: the intra tail (`decode.rs:18684+`) already reads 4 TX_64X64 luma leaves via
  `read_block_tx_size`, but codes ONE whole-block chroma unit afterwards. Move chroma INTO the leaf
  loop per 64x64 mu chunk, exactly as the inter branch does at `decode.rs:17304-17520`
  (chunk = `(row/16, col/16)`, chroma unit TX_32X32, `txb_skip` offset-10 rows, per-unit
  `record_mi_chroma`). Then drop the `side > 64 && !is_inter` refusal and un-`#[ignore]`
  `a_real_aomenc_inter_128x128_none_root_decodes_pixel_exact` (stream.rs).
- Sibling gates to re-run before claiming root cause: `128x128`, `refusal_inventory`,
  `gate_coverage`, plus the sb128b key-frame 128 gates.

Recipe search (seeds 62..67, same recipe) found no stream with the inter 128 NONE but without the
intra 128; all six stop on other lanes' gaps. Do not re-run it.
