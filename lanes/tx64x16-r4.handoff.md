# lane-tx64x16 r4 handoff (tool-call cap)

Branch `lane-tx64x16`, tip `82a486c` (on top of r3's `9e5c27d`):
- `774e175` — the gate un-ignored + the fixture that fires.
- `82a486c` — refusal message carries the tx depth, `lanes/tx64x16-r4.report.md`.

## The fixture that fires (TASK 1, done)

`gradients` never wins a 32-level 1:4 shape. 8-pixel BANDS do — a 32x8 strip is
flat, a 32x16 one straddles two bands:

    color=c=gray:s=192x128:d=0.04:r=25,format=gray,
    geq=lum='mod(floor(Y/8)*STEP,256)',noise=alls=N:all_seed=SEED,format=yuv420p

`X` instead of `Y` gives VERT_4. The `noise` layer is REQUIRED: without it the
flat bands trip the screen-content refusal. aomenc recipe = the gate's r3 one
(`--enable-1to4-partitions=1 --enable-rect-partitions=1 --enable-ab-partitions=0
--min-partition-size=8 --max-partition-size=32 --sb-size=64 --cpu-used=0`), cq
32/45, no repeated flags.

Per-attempt counts (`decode_probe` now prints `rect4_32: horz/vert/coded`;
`stream::rect4_32_counters()`), 192x128, one frame:

| source | cq | depth | horz_4 | vert_4 | coded | decode |
|---|---|---|---|---|---|---|
| bands Y, noise 6, seed 42 | 32 | 8-bit | 16 | 0 | 16 | OK |
| bands Y, noise 6, seed 42 | 32 | 10-bit | 16 | 0 | 16 | OK |
| bands X, noise 6, seed 42 | 32 | 8-bit | 0 | 24 | 24 | OK |
| bands X, noise 6, seed 42 | 32/45/55 | 10-bit | 0 | 24 | 24 | OK |
| bands Y, noise 14, seed 7 | 45/55 | 8+10-bit | 16 | 0 | 16 | OK |

Determinism (charter: hash twice): bands Y / noise 6 / seed 42 / cq32 / 8-bit
y4m sha256 `a1412882a9d79befe98a039a83081a47276aa38c7814ae4de8a83c520bab9fea`
twice; aomenc obu sha256
`47509854c7e9142c3268b48de9b84e1cee0c58d8b87fd382f05de748593bd6f9` twice.
Fully synthetic — NO film file and no `fixtures/` entry is needed at test time.

## The gate (TASK 2, done)

`stream.rs:13161 a_real_aomenc_stream_with_a_32x32_level_1to4_partition_decodes_pixel_exact`
is UN-IGNORED, asserts unchanged. Result (`cargo test -p ec-av1 --lib -- <name>
--nocapture`, EC_AV1_REQUIRE_AOMENC=1): **ok** —
8-bit 7/8 attempts pixel-exact, horz_4=64, vert_4=72, coded=136, 0 out of
scope, 0 mismatched, 1 named refusal; 10-bit 5/8 pixel-exact, 48/48/96, 0 out
of scope, 3 named refusals. `gate_coverage`: `enable-1to4-partitions` removed
from `NEVER_EXERCISED_10BIT`. r3's ungated lift of "a 32x32 partition type this
decoder does not code (value=8)" is now gated.

## What is left

1. TASK 3 (4:1 split transform) NOT started, deliberately: depth 1 =
   TX_16X8/TX_8X16 (rectangular unit, no `TxbSet::LumaRect16x8` on this branch —
   owed to the rectx merge); depth 2 = TX_8X8 square, but `decode_rect_split`
   addresses its block by coarse 16-px cell (`px = c * SUB`) and cannot name
   strips 1/3 of a 1:4 block — it needs an mi-granular `at`. The two depths
   INTERLEAVE (12/24 attempts stop at depth 1 first, 10/24 at depth 2), so a
   depth-2-only port cannot be gated. Fixture for that lane IS in hand: the band
   recipe above + `--enable-tx-size-search=1` at cq 12..28.
2. Full-suite totals OWED. The run (`setsid -f`, log
   `$HOME/.cache/tx64-suite-r4.log`) was still going at the cap: 173 `... ok`,
   **0 FAILED**, 0 panics, including this lane's gate and both split-transform
   sibling gates. A first attempt died at 296 ok / 0 failed when a foreground
   poll loop's SIGTERM hit its process group — start suites with `setsid -f`.
3. Films on this tip: hg (2160p 10-bit extract) → "a 32x32-level 1:4 strip with
   a split transform (... depth=2)"; troy (1080p extract) → "a coded HORZ/VERT
   strip whose chroma transform has no rect coefficient tables here"
   (unchanged).
4. Separate capability, out of this lane: the INTER 32x32 dispatch
   (`decode.rs:16720`) has NO 1:4 arm — re-encoded inter film crops refuse with
   "an INTER 32x32 partition type this decoder does not code (value=9)".
