# lane-sb128c r1 -> r2 handoff

Tip `31d4d68` on `lane-sb128c` (worktree `/home/tahinli/Documents/Code/Rust/edith_codecs-sb128c`,
`CARGO_TARGET_DIR=$HOME/.cache/cargo-target-sb128c`). Full detail: `lanes/sb128c-r1.report.md`.

ONE open defect: the 128x128 inter PARTITION_NONE block desyncs.

Repro (both files already exist, regenerate with the recipe in the report):
- stream `$HOME/.cache/sb128c-work/g8.obu` (256x256, 8 frames, `--sb-size=128 --cpu-used=0
  --max-partition-size=128 --min-partition-size=32 --cq-level=62 --enable-rect-partitions=0
  --enable-ab-partitions=0 --enable-1to4-partitions=0 --enable-palette=0 --enable-intrabc=0
  --enable-tx-size-search=0 --deltaq-mode=0 --limit=8`, gradients seed 61 + noise alls=6)
- `decode_probe g8.obu` -> refuses `a split intra strip whose transform unit is 32x64`, which the
  encoder cannot have coded (rect partitions off) => our desync.
- `EC_AV1_TRACE=1 decode_probe g8.obu | grep partition_w128` -> 4 x `value=0` (NONE), 10 x `value=3`.
- `k1.obu` (same recipe, `--limit=1`) decodes OK, so the key frame's 128 path is fine; the defect is on
  an inter frame.

Method: instrumented aomdec `EC_TRACE_COEFF` vs our `EC_TRACE_COEFF`, compare msac RANGE element by
element (never `tell()`), starting at the FIRST inter-frame 128 NONE block.

Suspect order (all untested):
1. `read_cdef` for a NONE 128 block on the INTER path (`CDEF_TRANSMITTED` reset + quadrant index).
   The key path resets it and replicates `CDEF_IDX_GRID`; the inter path now does the same, unverified.
2. `maybe_read_delta_q` / delta_lf granularity at a 128 root on an inter frame.
3. mvstack with bw4 = bh4 = 32 (`has_top_right` at 128, tpl sample positions, `check_sb_border`
   hardcodes 64 which the ledger says is correct at both sb sizes).
4. This round's own mu-chunk chroma interleave inside `decode_inter_block`: the `Some(3)` chroma
   `txb_skip` offset row, the per-unit `record_mi_chroma`, and the chunk-completion test
   (`(leaves[idx+1].0/16, leaves[idx+1].1/16) != chunk`).

Un-`#[ignore]` `a_real_aomenc_inter_128x128_none_root_decodes_pixel_exact` (stream.rs) when green, and
re-run the siblings `128x128` + `refusal_inventory` + `gate_coverage` before claiming root cause.
