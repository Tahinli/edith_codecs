# lane-t900 r7 handoff -- frame-14 chroma basis CLOSED; next stop is t900 frame 41 (ONE U sample)

Branch `lane-t900`, tip `5c2ab6a5` (fix+gate `04154beb`, report `5c2ab6a5`),
main `3b529205` merged (fast-forward). Suite on this tip: **426 passed, 0
failed, 38 ignored** (`$HOME/.cache/t900-suite-r7.log`).

## Closed this round

Sub-8x8 inter groups: the one chroma unit inherits the CHROMA-REFERENCE (last)
sub-block's first transform unit's `tx_type`, because `av1_get_tx_type` indexes
`xd->tx_type_map`, which `set_mi_offsets` (av1_common_int.h:1679) re-points to
the CURRENT sub-block. `decode.rs:23614` (`i == 3`) and `decode.rs:24598`
(`idx == 0 && i == 1`). Counter `sub8_chroma_tx_from_ref_hits`.

Witness fixture EXTENDED (same path): 23 frame-carrying OBUs, 177773 bytes,
sha256 `09d995946d27695ef4b7b1a24a4d7ad85f8e2c8176d58ee54e825efc061f9cbc`,
16 decode-order frames, 15 shown. `trunc.py <2s cut> 23`.

## Instruments (reuse, do not rebuild)

* Oracle `~/.cache/t900-tmp/aom-build/aomdec` -- private build of the SHARED
  source with the additive `EC_TRACE_TXTYPE` rung
  (`decodetxb.c:187`: `EC_TXTYPE mi=<row>,<col> plane= br= bc= txs= tx_type=`).
  Only prints for units that are not `all_zero` (libaom returns at
  `decodetxb.c:172` before it).
* Ours: `EC_TRACE_TXTYPE=1` -> `OUR_TXTYPE plane= x= y= (side=|w= h=) tx_type=
  inh=` (`decode.rs:14695` rect, `decode.rs:18480` square). `x`/`y` are that
  plane's own pixel coords.
* `~/.cache/t900-tmp/`: `pf.sh` (prefilter compare), `px.py` (per-plane diff;
  RUN IT FROM `~/.cache/t900-tmp`, its paths are relative), `cmp.sh`
  (decode-order final compare), `sb.py`, `lad.py`, `reg.py`, cuts
  `cut_{900,5400,6300,8100}.obu` (2 s each) and dumps `d_r7_*`.

## Next defect, already localised to one sample

t900 cut, decode-order frame 41: ONE U sample differs, chroma (354,395),
aomdec 490 vs ours 491. y=395 is the LAST chroma row (792/2 = 396). No cascade
-- frame 42 is exact. Start with `pf.sh` on that cut: if the PREFILTER frame 41
is exact, it is a post-filter (deblock/CDEF/LR) bottom-edge rounding, which is
a different class from every r2-r7 fix.

Then, each its own round: t5400 frame 33 (1642397 bytes), t6300 frame 11 (35
bytes -- small, likely another basis/rounding defect), t8100 frame 1 (1313
bytes). All four cuts now decode ALL frames; no refusal stops any of them, and
`~/.cache/troy-0-10s.obu` decodes 240 frames with no env switches.

`intra128_in_inter` fires 0 times on every stream tried, so its refusal stays
un-lifted for want of a witness (do not lift it without one).
