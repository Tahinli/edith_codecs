# lane-t900 r14 handoff -- 1080p is clean; next frontier is 4K

Tip `78347fca` on `lane-t900` (base main `a870796b`, merged fast-forward at round start).
Report: `lanes/t900-r14.report.md`.

## Closed this round (do not re-measure)

* Root cause: OBMC neighbour `op_mi_size` is `AOMMIN(xd->width|height, mi_step)`
  (`obmc.h:52,83`), never the `end_col`/`end_row` frame-edge clamp. Our clamp shrank the
  neighbour PREDICTION, which at 4 chroma rows also flipped `mc.rs:418` to the 4-tap
  kernel -> 1-off on the last valid chroma row. Fixed in `a6c408b7`; witness counter
  `obmc_edge_span_hits` asserted (33 hits) on the 128-SB inter gate in `52a11095`.
* Every 1080p offset probed is byte-exact: head 0-10 s (241 f), 300/900/1200/1800/2100/
  3000/3900/4800/5400/5700/6300/6600/7500/8100/8400/9300/9900/10200 s. Compare script
  `~/.cache/t900-tmp/cmp13.sh <name> <obu>`; sweep driver `~/.cache/t900-tmp/sweep14.sh`,
  log `~/.cache/t900-tmp/sweep14.log`.
* 4K re-check: `h2700.obu` 107 frames and `h1500.obu` 244 frames, 0 differing.
* Suites (both green, no FAILED): r14 fix 429 passed/0 failed/38 ignored in 861.07s
  (`$HOME/.cache/t900-suite-r14.log`); final tip 429/0/38 in 820.42s
  (`$HOME/.cache/t900-suite-r14b.log`).

## Owed

* The 4K census8 offsets that were not exact before this round (24 of them, `run8.sh`)
  were NOT re-swept -- two spot cuts are exact, the rest is unmeasured. Start there:
  retarget `run8.sh`'s `PROBE` to `$HOME/.cache/cargo-target-t900b/release/examples/
  decode_probe` and run the 4K offsets, then ladder the first bad frame with
  `EC_AV1_FINAL_DUMP` + `EC_MCB`/`EC_OBMC`/`EC_PRED` as in r13/r14.

## Rungs (all additive, env-gated, on both decoders)

`EC_MCB="<plane>:<px>:<py>[:<frame>]"` -- aomdec prints every
`dec_build_inter_predictors` call covering that pixel (obmc=1 tmp buffers included);
ours prints the block's chroma rows pre/post blend AND, new in r14, each LEFT-pass
neighbour's tmp rows with `off4/span4/cbw/cbh/coy/mv/ref0`.
