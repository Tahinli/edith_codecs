[package]
name = "ec-dsp"
description = "Shared transforms for the edith_codecs family: FFT, DCT, MDCT and analysis windows."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
# For the colour matrices `remap` converts between; no other IR is used here.
ec-core.workspace = true
wide.workspace = true
