[package]
name = "ec-vorbis"
description = "Vorbis I decoder and encoder: full codebook/floor/residue decode, MDCT via ec-dsp, and an encoder with native mono/stereo/surround setup headers."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
ec-core.workspace = true
ec-dsp.workspace = true

[dev-dependencies]
ec-ogg = { path = "../ec-ogg", version = "0.1.0" }
