[package]
name = "ec-inflate"
description = "DEFLATE (RFC 1951) and zlib (RFC 1950) decompression with a hard ceiling on the output."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
ec-core.workspace = true

[dev-dependencies]
flate2 = "1"
