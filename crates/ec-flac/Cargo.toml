[package]
name = "ec-flac"
description = "FLAC decoder and encoder (RFC 9639), built on the edith_codecs IR."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
ec-core = { workspace = true }
