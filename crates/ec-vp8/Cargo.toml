[package]
name = "ec-vp8"
description = "Native VP8 decoder (RFC 6386) — key-frame and inter-frame decoding of the full format, replacing the runtime-dlopen'd libvpx seat."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
ec-core.workspace = true
