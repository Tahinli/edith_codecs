[package]
name = "ec-subs"
description = "Text subtitle parsing for the edith_codecs family: SubRip and WebVTT into one cue IR."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
ec-core.workspace = true

[dev-dependencies]
ec-ass = { path = "../ec-ass", version = "0.1.0" }
ec-pgs = { path = "../ec-pgs", version = "0.1.0" }
