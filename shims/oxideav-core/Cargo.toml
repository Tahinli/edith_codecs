[package]
name = "oxideav-core"
description = "Compatibility shim: the oxideav-core surface edith consumes, over ec-core."
version = "0.1.34"
edition = "2024"
license = "MIT OR Apache-2.0"
authors = ["Ahmet Kaan Gümüş"]
publish = false

[dependencies]
ec-core = { path = "../../crates/ec-core", version = "0.1.0" }
